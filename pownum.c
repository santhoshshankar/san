#include<stdio.h>
int main()
{
int base,exp;
a=10;
printf("enter the base:");
scanf("%d",&b);
printf("enter the exponent:");
scanf("%d",&e);
while (e != 0)
    {
        result *=e;
        b--;
    }

    printf("Answer = %d", a);

    return 0;
}
